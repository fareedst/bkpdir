ARCH-INCREMENTAL_DUPLICATE_PREVENTION:
  alternatives_considered:
    - cons: []
      name: See detail file
      pros: []
      rejected_reason: |-
        Manual, non-data-driven sketches: rejected (drift risk, no repeatability).
        Single static diagram only: rejected (does not convey lifecycle/time dimension or evolving token story).
  always_create_incremental: Rejected - wastes disk space and creates clutter
  animation/timeline_module: Render an animated sequence or stepwise storyboard that highlights token handoffs over time (REQ → ARCH → IMPL → TEST/CODE), with on-frame token callouts.
  change_detection: Use same change detection logic as diff command
  compare_only_against_full_archive: Rejected - allows duplicate incrementals with no changes
  consequences:
    - A canonical `docs/package-interdependency-mapping.md` is required and serves as the authoritative mapping for EXTRACT-008.
    - Where coupling is unavoidable, introduce small interfaces to break cycles and record these interface contracts in the mapping document and in `implementation-decisions.md`.
  cross-references: '[REQ-STDD_VIS], [REQ-MODULE_VALIDATION]'
  cross_references:
    - REQ-DIFF_COMMAND
    - REQ-EXTRACT_008_INTERDEP_MAPPING
    - REQ-INCREMENTAL_DUPLICATE_PREVENTION
    - REQ-MAINTAINABILITY
    - REQ-MODULE_VALIDATION
    - REQ-OUTPUT_FORMATTING
    - REQ-STDD_VIS
  data_extraction_module: 'Parse `tied/semantic-tokens.md`, `requirements.md`, `architecture-decisions.md`, `implementation-decisions.md`, and sampled code/test anchors to build a token graph (nodes: REQ/ARCH/IMPL/TEST/CODE; edges: traceability links) with token metadata (type, status, sample refs) for visual prominence.'
  decision: Use diff command analysis to prevent duplicate incremental archives
  implementation_approach:
    details: []
    integration_points: |-
      1. **Incremental Archive Creation**: Modify `createIncrementalArchive` to use diff analysis
      2. **Archive State Reconstruction**: Share reconstruction logic with diff command
      3. **Comparison Engine**: Reuse comparison logic from diff command
      4. **Output Formatting**: Use consistent format strings for skip messages
    summary: Use diff command analysis to prevent duplicate incremental archives
  integration_&_storage: Export assets to `docs/images/` (SVG/PNG/GIF/MP4 as needed) and embed into STDD docs with captions and token callouts; include a data snapshot (JSON/CSV) to keep visuals reproducible and token-accurate.
  integration_points: |-
    1. **Incremental Archive Creation**: Modify `createIncrementalArchive` to use diff analysis
    2. **Archive State Reconstruction**: Share reconstruction logic with diff command
    3. **Comparison Engine**: Reuse comparison logic from diff command
    4. **Output Formatting**: Use consistent format strings for skip messages
  metadata:
    created:
      author: MCP converter
      date: '2026-03-12'
    last_updated:
      author: MCP converter
      date: '2026-03-12'
      reason: Converted from monolithic
    last_validated:
      date: '2026-03-12'
      result: pass
      validator: MCP converter
  module_boundaries_(for_[req-module_validation],_token_emphasis):
    - '`AnimationTimeline`: input = ordered trace steps; output = animated timeline/multi-frame storyboard with token callouts per step.'
    - '`DataExtraction`: input = STDD docs + selected code/test anchors; output = normalized token graph/JSON with token metadata for styling.'
    - '`Integration`: links assets into docs, preserves token prominence guidance, and records validation notes.'
    - '`Visualization`: input = token graph; output = static layered diagram with tokens visually dominant; legends articulate token roles.'
  name: Incremental Archive Duplicate Prevention Architecture
  rationale:
    benefits: []
    problems_solved: []
    why: |-
      Stakeholders need a concise, token-centric narrative of how STDD preserves intent across layers.
      A data-driven pipeline (sourced from existing `tied/*.md` plus representative code/test anchors) keeps token prominence accurate as the registry evolves.
      Complementary artifacts (layered flow + timeline/animation) address both static comprehension and process storytelling while keeping tokens in the spotlight.
  related_decisions:
    depends_on:
      - REQ-DIFF_COMMAND
      - REQ-EXTRACT_008_INTERDEP_MAPPING
      - REQ-INCREMENTAL_DUPLICATE_PREVENTION
      - REQ-MAINTAINABILITY
      - REQ-MODULE_VALIDATION
      - REQ-OUTPUT_FORMATTING
      - REQ-STDD_VIS
    informs: []
    see_also: []
  reuse_diff_logic: Leverage diff command comparison engine for duplicate detection
  separate_comparison_logic: Rejected - code duplication and potential inconsistency
  skip_creation: Skip incremental archive creation if no changes detected
  state_reconstruction: Reconstruct effective state from full + most recent incremental archive
  status: Active
  traceability:
    code_annotations:
      - ARCH-INCREMENTAL_DUPLICATE_PREVENTION
      - REQ-DIFF_COMMAND
      - REQ-EXTRACT_008_INTERDEP_MAPPING
      - REQ-INCREMENTAL_DUPLICATE_PREVENTION
      - REQ-MAINTAINABILITY
      - REQ-MODULE_VALIDATION
      - REQ-OUTPUT_FORMATTING
      - REQ-STDD_VIS
    implementation: []
    requirements:
      - REQ-DIFF_COMMAND
      - REQ-EXTRACT_008_INTERDEP_MAPPING
      - REQ-INCREMENTAL_DUPLICATE_PREVENTION
      - REQ-MAINTAINABILITY
      - REQ-MODULE_VALIDATION
      - REQ-OUTPUT_FORMATTING
      - REQ-STDD_VIS
    tests: []
  user_feedback: Display appropriate message when archive creation is skipped
  visualization_module: Generate a layered flow (e.g., swimlane/graph or Sankey-style) where tokens are the dominant nodes (color/weight/labels), edges clarify trace paths, and legends explain token roles.
