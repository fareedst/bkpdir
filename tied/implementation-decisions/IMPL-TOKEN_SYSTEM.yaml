IMPL-TOKEN_SYSTEM:
  name: Token System Implementation
  status: Active
  cross_references:
  - ARCH-TOKEN_SYSTEM
  - ARCH-RESOURCE_MANAGEMENT
  - REQ-DOC_016
  - REQ-FILE_BACKUP
  - REQ-RESOURCE_MANAGEMENT
  - REQ-GOV_REGISTRY_COMPLETENESS
  - REQ-IMMUTABLE_DIRECTORY_OPERATIONS
  rationale:
    why: 'Ensures complete traceability from requirements through architecture to implementation

      Enables AI assistant navigation and feature discovery

      Supports automated validation of token consistency

      Maintains long-term code maintainability'
    problems_solved: []
    benefits: []
  implementation_approach:
    summary: "1. **Structured Extraction**\n   - Use `.venv/bin/python` helpers (PyYAML) to parse `project-tokens.yaml` and emit normalized JSON for each group (`actions`, `features`, `semantic_tokens`).\n   - Capture metadata (description, status, source paths, subtasks) for downstream documentation.\n2. **Cross-Link Synthesis**\n   - For each legacy token, resolve canonical `[REQ-*]`, `[ARCH-*]`, `[IMPL-*]`, and test references using prefix heuristics (e.g., `DIR-001 → [REQ-IMMUTABLE_DIRECTORY_OPERATIONS]`).\n   - Flag gaps by opening subtasks in `tied/tasks.md` whenever a requirement/decision/test link is missing.\n3. **Registry Authoring**\n   - Generate Markdown tables per token category with columns: Description, Linked Requirement(s), Linked Architecture, Linked Implementation, Linked Tests/Code, Source Document, Status.\n   - Embed migration status badges so governance tooling can verify coverage.\n4. **YAML Decommissioning**\n   - After each category is represented in markdown,\
      \ replace the YAML entries with a pointer stub until the file can be removed in a final cleanup change. `project-tokens.yaml` now contains a canonical pointer to `tied/semantic-tokens.md`.\n5. **Validation Hooks**\n   - Run `scripts/validate-token-traceability.sh`, `scripts/token-coverage-analysis.sh`, and targeted `rg \"\\[TOKEN\\]\"` sweeps to prove each migrated token exists in requirements, architecture decisions, implementation notes, code, and tests.\n   - Record validation results inside the migration tables for auditability."
    details: []
  code_locations:
    files: []
    functions: []
  traceability:
    architecture:
    - ARCH-TOKEN_SYSTEM
    - ARCH-RESOURCE_MANAGEMENT
    requirements:
    - REQ-DOC_016
    - REQ-FILE_BACKUP
    - REQ-RESOURCE_MANAGEMENT
    - REQ-GOV_REGISTRY_COMPLETENESS
    - REQ-IMMUTABLE_DIRECTORY_OPERATIONS
    tests: []
    code_annotations:
    - IMPL-TOKEN_SYSTEM
    - ARCH-TOKEN_SYSTEM
    - ARCH-RESOURCE_MANAGEMENT
    - REQ-DOC_016
    - REQ-FILE_BACKUP
    - REQ-RESOURCE_MANAGEMENT
    - REQ-GOV_REGISTRY_COMPLETENESS
    - REQ-IMMUTABLE_DIRECTORY_OPERATIONS
  related_decisions:
    depends_on: []
    supersedes: []
    see_also:
    - ARCH-TOKEN_SYSTEM
    - ARCH-RESOURCE_MANAGEMENT
    - REQ-DOC_016
    - REQ-FILE_BACKUP
    - REQ-RESOURCE_MANAGEMENT
    - REQ-GOV_REGISTRY_COMPLETENESS
    - REQ-IMMUTABLE_DIRECTORY_OPERATIONS
    composed_with: []
  metadata:
    created:
      date: '2026-03-12'
      author: MCP converter
    last_updated:
      date: '2026-03-12'
      author: AI agent
      reason: Enriched pseudo-code per PROC-IMPL_CODE_TEST_SYNC
    last_validated:
      date: '2026-03-12'
      validator: MCP converter
      result: pass
  code_markers: '`project-tokens.yaml` migration scripts, `tied/semantic-tokens.md` tables annotated with `[REQ-DOC_016]`, validation logs appended to docs/governance summaries.'
  decision: Implement a deterministic migration workflow that relocates every legacy token entry from `project-tokens.yaml` into `tied/semantic-tokens.md`, generating STDD-compliant cross-links during the transfer.
  migration_outcome:
  - All legacy tokens were migrated into `tied/semantic-tokens.md` and validated via `scripts/token-coverage-analysis.sh` and `scripts/validate-token-traceability.sh`.
  - 'Validation report: `docs/validation-reports/token-coverage-analysis.md` (100% source/test/overall coverage).'
  - '`project-tokens.yaml` replaced with a pointer stub to the canonical STDD registry.'
  - 'Created implementation marker: `[IMPL-TOKEN_MIGRATION_COMPLETE]` to indicate the migration is finished and audited.'
  pseudo-code: '```bash

    # Extraction artifacts archived in docs/governance and migration logs

    .venv/bin/python scripts/token_inventory.py > /tmp/tokens.json

    .venv/bin/python scripts/render_token_tables.py --group features >> tied/semantic-tokens.md

    scripts/validate-token-traceability.sh

    scripts/token-coverage-analysis.sh

    ```'
  token_usage_in_code: "```go\n// [REQ-FILE_BACKUP] Create backup of single file with comparison\n// [IMPL-ATOMIC_OPS] [ARCH-RESOURCE_MANAGEMENT] [REQ-RESOURCE_MANAGEMENT]\nfunc CreateFileBackup(cfg *Config, filePath string, note string, dryRun bool) error {\n    // ...\n}\n```"
  token_usage_in_tests: "```go\n// Test validates [REQ-FILE_BACKUP] is met\nfunc TestCreateFileBackup_REQ_FILE_BACKUP(t *testing.T) {\n    // ...\n}\n```"
  validation_implementation:
  - 'Token format validation: Check `[TYPE:IDENTIFIER]` pattern compliance'
  - 'Cross-layer consistency: Verify tokens exist in all required layers'
  - 'Traceability validation: Ensure all requirements have implementation tokens'
  - 'Missing token detection: Identify features without proper token coverage'
  essence_pseudocode: "# [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM] [ARCH-RESOURCE_MANAGEMENT] [REQ-DOC_016] [REQ-FILE_BACKUP] [REQ-RESOURCE_MANAGEMENT] [REQ-GOV_REGISTRY_COMPLETENESS] [REQ-IMMUTABLE_DIRECTORY_OPERATIONS] — Deterministic migration of legacy tokens to STDD canonical registry with full traceability\n\nPROCEDURE structured_extraction():\n  # [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM] [REQ-DOC_016] — Parse legacy token YAML and emit normalized JSON per group\n  parse project-tokens.yaml using PyYAML helpers\n  for each group in [actions, features, semantic_tokens]:\n    emit normalized JSON with metadata (description, status, source_paths, subtasks)\n  archive extraction artifacts in docs/governance/\n\n# [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM] [REQ-DOC_016] — Block implements documented behavior for: PROCEDURE cross_link_synthesis():\nPROCEDURE cross_link_synthesis():\n  # [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM] [REQ-GOV_REGISTRY_COMPLETENESS] [REQ-IMMUTABLE_DIRECTORY_OPERATIONS]\
    \ — Resolve canonical token references for each legacy token\n  for each legacy_token in extracted_tokens:\n    resolve canonical [REQ-*], [ARCH-*], [IMPL-*] references using prefix heuristics\n    # [REQ-FILE_BACKUP] [REQ-RESOURCE_MANAGEMENT] — Map domain identifiers to STDD tokens [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM]\n    map legacy identifiers to STDD tokens (e.g., DIR-001 -> REQ-IMMUTABLE_DIRECTORY_OPERATIONS)\n    if any requirement/decision/test link is missing:\n      flag gap and open subtask in tasks tracking\n\n# [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM] [REQ-DOC_016] — Block implements documented behavior for: PROCEDURE registry_authoring():\nPROCEDURE registry_authoring():\n  # [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM] [REQ-DOC_016] [ARCH-RESOURCE_MANAGEMENT] — Generate Markdown tables per token category\n  DATA table_columns: [Description, Linked_REQ, Linked_ARCH, Linked_IMPL, Linked_Tests, Source_Document, Status]\n  for each token_category in extracted_groups:\n    generate\
    \ Markdown table with table_columns\n    embed migration status badges for governance verification\n  write tables to tied/semantic-tokens.md\n\n# [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM] [REQ-DOC_016] — Block implements documented behavior for: PROCEDURE yaml_decommissioning():\nPROCEDURE yaml_decommissioning():\n  # [IMPL-TOKEN_SYSTEM] [ARCH-RESOURCE_MANAGEMENT] [REQ-GOV_REGISTRY_COMPLETENESS] — Replace legacy YAML with pointer stubs\n  for each migrated_category:\n    replace YAML entries in project-tokens.yaml with pointer stub\n  final project-tokens.yaml contains canonical pointer to tied/semantic-tokens.md\n\n# [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM] [REQ-DOC_016] — Block implements documented behavior for: VALIDATE token_migration():\nVALIDATE token_migration():\n  # [IMPL-TOKEN_SYSTEM] [ARCH-TOKEN_SYSTEM] [REQ-GOV_REGISTRY_COMPLETENESS] [REQ-DOC_016] — Prove each token exists across all layers\n  CHECK run scripts/validate-token-traceability.sh passes\n  CHECK run scripts/token-coverage-analysis.sh\
    \ shows 100% coverage\n  CHECK each migrated token exists in requirements, architecture, implementation, code, and tests\n  CHECK record validation results in migration tables for auditability"
