IMPL-TIED_FILES:
  name: TIED File Creation
  status: Active
  cross_references:
  - ARCH-TIED_STRUCTURE
  - REQ-TIED_SETUP
  rationale:
    why: Implements ARCH-TIED_STRUCTURE and realizes REQ-TIED_SETUP by creating the foundational TIED directory structure and files
    problems_solved:
    - Manual setup of TIED structure is error-prone
    - Inconsistent project structure across implementations
    - Missing required TIED files and directories
    benefits:
    - Automated, consistent TIED setup
    - All required files and directories created correctly
    - Easy project bootstrapping with single command
  implementation_approach:
    summary: Bootstrap script that creates tied/ directory, copies template files, and sets up base files
    details:
    - Create tied/ directory at project root
    - Copy YAML index files from templates (requirements.yaml, architecture-decisions.yaml, implementation-decisions.yaml, semantic-tokens.yaml)
    - Copy guide files from templates (semantic-tokens.md, processes.md, commit-guidelines.md)
    - Create detail subdirectories (requirements/, architecture-decisions/, implementation-decisions/)
    - Copy base files to project root (.cursorrules, AGENTS.md, ai-principles.md)
  code_locations:
    files:
    - path: copy_files.sh
      description: Bootstrap script that sets up TIED structure
      lines:
      - 1
      - 352
  traceability:
    architecture:
    - ARCH-TIED_STRUCTURE
    requirements:
    - REQ-TIED_SETUP
    tests:
    - Manual verification checklists
    code_annotations:
    - IMPL-TIED_FILES
    - ARCH-TIED_STRUCTURE
    - REQ-TIED_SETUP
  related_decisions:
    depends_on: []
    supersedes: []
    see_also:
    - ARCH-TIED_STRUCTURE
    - REQ-TIED_SETUP
  metadata:
    created:
      date: 2025-12-18
      author: AI Agent
    last_updated:
      date: 2026-03-27
      author: AI Agent
      reason: copy_files.sh line range; remove phantom function entry [IMPL-TIED_FILES]
    last_validated:
      date: 2026-02-06
      validator: AI Agent
      result: pass
  essence_pseudocode: "# [IMPL-TIED_FILES] [ARCH-TIED_STRUCTURE] [REQ-TIED_SETUP] — Bootstrap script that creates tied/ directory, copies template files, and sets up base files\n\nprocedure tied_file_creation():\n  # [IMPL-TIED_FILES] [ARCH-TIED_STRUCTURE] [REQ-TIED_SETUP] Create tied/ directory at project root\n  create_tied_directory_at()\n  # [IMPL-TIED_FILES] [ARCH-TIED_STRUCTURE] [REQ-TIED_SETUP] Copy YAML index files from templates (requirements.yaml, architecture-decisions.yaml, implementation-decisions.yaml, semantic-tokens.yaml)\n  copy_yaml_index_files()\n  # [IMPL-TIED_FILES] [ARCH-TIED_STRUCTURE] [REQ-TIED_SETUP] Copy guide files from templates (semantic-tokens.md, processes.md, commit-guidelines.md)\n  copy_guide_files_from()\n  # [IMPL-TIED_FILES] [ARCH-TIED_STRUCTURE] [REQ-TIED_SETUP] Create detail subdirectories (requirements/, architecture-decisions/, implementation-decisions/)\n  create_detail_subdirectories_requirements()\n  # [IMPL-TIED_FILES] [ARCH-TIED_STRUCTURE] [REQ-TIED_SETUP]\
    \ Copy base files to project root (.cursorrules, AGENTS.md, ai-principles.md)\n  copy_base_files_to()\n  end"
