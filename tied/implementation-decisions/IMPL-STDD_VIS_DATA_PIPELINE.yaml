IMPL-STDD_VIS_DATA_PIPELINE:
  code_locations:
    files:
      - docs/data/stdd-samples.csv
      - docs/data/stdd-trace.json
    functions: []
  cross_references:
    - ARCH-STDD_VIS_FLOW
    - REQ-MODULE_VALIDATION
    - REQ-STDD_VIS
  implementation_approach:
    details:
      - Capture token metadata (type, status, sample refs) for token prominence in visuals
      - 'Export artifacts: docs/data/stdd-trace.json (graph) and docs/data/stdd-samples.csv (sample chains)'
      - 'Input sources: semantic-tokens.md, requirements.md, architecture-decisions.md, implementation-decisions.md, and sampled code/test anchors'
      - 'Module boundary: DataExtraction outputs normalized graph once; downstream modules consume without re-parsing'
      - Normalize nodes by type (REQ/ARCH/IMPL/TEST/CODE) and emit edges for cross-references
      - 'Validation hooks: schema check, count consistency vs token registry, spot-check two sample chains'
    summary: Input STDD documents, normalize token nodes by type, emit cross-reference edges, export graph JSON and sample CSV
  metadata:
    author: AI Agent
    created: '2026-03-12'
    last_updated:
      author: AI agent
      date: '2026-06-02'
      reason: Track C bulk sidecar migration from inline essence
  name: STDD Visualization Data Pipeline
  rationale:
    benefits:
      - Repeatable regeneration when tokens or docs change
      - Reviewable JSON/CSV snapshot for validation
      - Synchronized visuals with canonical token registry
    problems_solved:
      - Lack of reviewable intermediate data for validation
      - Manual visualization creation that cannot be repeated
      - Visuals becoming stale when tokens change
    why: Keeps visuals synchronized with canonical token registry, enables regeneration when tokens or docs change, provides reviewable JSON/CSV snapshot
  related_decisions:
    depends_on: []
    see_also:
      - IMPL-STDD_VIS_ASSETS
    supersedes: []
  status: Active
  traceability:
    architecture:
      - ARCH-STDD_VIS_FLOW
    code_annotations: []
    requirements:
      - REQ-MODULE_VALIDATION
      - REQ-STDD_VIS
    tests: []
