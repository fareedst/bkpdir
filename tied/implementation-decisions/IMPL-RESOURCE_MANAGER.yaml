IMPL-RESOURCE_MANAGER:
  name: Resource Management with Cleanup
  status: Active
  cross_references:
    - ARCH-RESOURCE_MANAGEMENT
    - REQ-RESOURCE_MANAGEMENT
  rationale:
    why: |-
      Prevents resource leaks on operation failure
      Handles panic scenarios gracefully
      Thread-safe for concurrent operations
      Simplifies error handling in operations
    problems_solved: []
    benefits: []
  implementation_approach:
    summary: Implement ResourceManager for automatic cleanup with panic recovery
    details: []
  code_locations:
    files: []
    functions: []
  traceability:
    architecture:
      - ARCH-RESOURCE_MANAGEMENT
    requirements:
      - REQ-RESOURCE_MANAGEMENT
    tests: []
    code_annotations:
      - IMPL-RESOURCE_MANAGER
      - ARCH-RESOURCE_MANAGEMENT
      - REQ-RESOURCE_MANAGEMENT
  related_decisions:
    depends_on: []
    supersedes: []
    see_also:
      - ARCH-RESOURCE_MANAGEMENT
      - REQ-RESOURCE_MANAGEMENT
    composed_with: []
  detail_file: implementation-decisions/IMPL-RESOURCE_MANAGER.yaml
  metadata:
    created:
      date: "2026-03-12"
      author: MCP converter
    last_updated:
      date: "2026-03-12"
      author: MCP converter
      reason: Converted from monolithic
    last_validated:
      date: "2026-03-12"
      validator: MCP converter
      result: pass
  code_markers: '`ResourceManager` type, `CleanupWithPanicRecovery()` calls'
  methods:
    - '`NewResourceManager()`: Create new resource manager'
    - '`AddTempFile(path string)`: Register temporary file for cleanup'
    - '`AddTempDir(path string)`: Register temporary directory for cleanup'
    - '`RemoveResource(resource Resource)`: Remove resource from tracking'
    - '`Cleanup()`: Clean up all registered resources'
    - '`CleanupWithPanicRecovery()`: Clean up with panic recovery'
  resourcemanager_structure: |-
    ```go
    type ResourceManager struct {
        tempFiles []*TempFile
        tempDirs  []*TempDir
        mutex     sync.RWMutex
    }

    type TempFile struct {
        Path string
    }

    type TempDir struct {
        Path string
    }
    ```
  essence_pseudocode: |-
    # [IMPL-RESOURCE_MANAGER] [ARCH-RESOURCE_MANAGEMENT] [REQ-RESOURCE_MANAGEMENT] — Implement ResourceManager for automatic cleanup with panic recovery

    procedure resource_management_with_cleanup():
      # [IMPL-RESOURCE_MANAGER] [ARCH-RESOURCE_MANAGEMENT] [REQ-RESOURCE_MANAGEMENT] Prevents resource leaks on operation failure
      execute()
      end
