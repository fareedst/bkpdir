IMPL-RESOURCE_MANAGER:
  code_locations:
    files:
      - pkg/resources/interfaces.go
    functions:
      - AddResource
      - AddTempDir
      - AddTempFile
      - Cleanup
      - CleanupIf
      - CleanupWithContext
      - CleanupWithPanicRecovery
      - GetResourceCount
      - GetResources
      - GetResourcesByType
      - NewResourceManager
      - RemoveResource
  code_markers: '`ResourceManager` type, `CleanupWithPanicRecovery()` calls'
  cross_references:
    - ARCH-RESOURCE_MANAGEMENT
    - REQ-RESOURCE_MANAGEMENT
  implementation_approach:
    details: []
    summary: |-
      Thread-safe tracking of temporary files and directories with automatic cleanup, panic recovery, and context-aware cancellation. Archive and backup creation uses atomic operations via coordinated temp-file lifecycle.
      INPUT: Resource instances, temp paths, optional context
      OUTPUT: cleanup errors, updated resource list
      DATA: ResourceManager slice guarded by mutex, TempFile and TempDir implementations
      Canonical behavior: tied/implementation-decisions/IMPL-RESOURCE_MANAGER-pseudocode.md
  metadata:
    created:
      author: MCP converter
      date: '2026-03-12'
    last_updated:
      author: AI agent
      date: '2026-09-20'
      reason: probe
    last_validated:
      date: '2026-03-12'
      result: pass
      validator: MCP converter
  methods:
    - '`AddTempDir(path string)`: Register temporary directory for cleanup'
    - '`AddTempFile(path string)`: Register temporary file for cleanup'
    - '`Cleanup()`: Clean up all registered resources'
    - '`CleanupWithPanicRecovery()`: Clean up with panic recovery'
    - '`NewResourceManager()`: Create new resource manager'
    - '`RemoveResource(resource Resource)`: Remove resource from tracking'
  name: Resource Management with Cleanup
  rationale:
    benefits: []
    problems_solved: []
    why: |-
      Prevents resource leaks on operation failure
      Handles panic scenarios gracefully
      Thread-safe for concurrent operations
      Simplifies error handling in operations
  related_decisions:
    composed_with: []
    depends_on: []
    see_also:
      - ARCH-RESOURCE_MANAGEMENT
      - REQ-RESOURCE_MANAGEMENT
    supersedes: []
  resourcemanager_structure: |-
    ```go
    type ResourceManager struct {
        tempFiles []*TempFile
        tempDirs  []*TempDir
        mutex     sync.RWMutex
    }

    type TempFile struct {
        Path string
    }

    type TempDir struct {
        Path string
    }
    ```
  status: Active
  traceability:
    architecture:
      - ARCH-RESOURCE_MANAGEMENT
    code_annotations:
      - ARCH-RESOURCE_MANAGEMENT
      - IMPL-RESOURCE_MANAGER
      - REQ-RESOURCE_MANAGEMENT
    requirements:
      - REQ-RESOURCE_MANAGEMENT
    tests:
      - pkg/resources/resources_test.go::TestResourceManager
      - pkg/resources/resources_test.go::TestResourceManager_CleanupIf
      - pkg/resources/resources_test.go::TestResourceManager_CleanupWithContext
      - pkg/resources/resources_test.go::TestResourceManager_CleanupWithPanicRecovery
      - pkg/resources/resources_test.go::TestResourceManager_ConcurrentAccess
      - pkg/resources/resources_test.go::TestResourceManager_RemoveResource
      - pkg/resources/resources_test.go::TestTempDir
      - pkg/resources/resources_test.go::TestTempFile
