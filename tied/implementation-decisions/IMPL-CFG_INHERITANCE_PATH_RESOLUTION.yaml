IMPL-CFG_INHERITANCE_PATH_RESOLUTION:
  added_expandpath_method: Added `ExpandPath` method to `defaultPathResolver` to handle `~` expansion
  code_changes:
    - '`config.go` line 2212-2240: Updated `resolvePath` to expand `~` and handle directories correctly'
    - '`config.go` line 2241-2253: Added `ExpandPath` method to `defaultPathResolver`'
    - '`config.go` line 2289: `buildChainRecursive(parentPath, filepath.Dir(resolvedPath), pathResolver, chain)`'
  code_locations:
    files:
      - description: Core path resolution and inheritance chain building for configuration files
        path: config.go
      - description: Extracted inheritance chain builder, path resolver, and circular dependency detector
        path: pkg/config/inheritance.go
    functions:
      - description: Expands ~ to home directory and environment variables in paths
        file: config.go
        name: ExpandPath
      - description: Extracted path expansion for ~ and environment variables
        file: pkg/config/inheritance.go
        name: ExpandPath
      - description: Extracted path resolver with ~ expansion and relative path joining
        file: pkg/config/inheritance.go
        name: ResolvePath
      - description: Extracted version; passes filepath.Dir(resolvedPath) as basePath for parent resolution
        file: pkg/config/inheritance.go
        name: buildChainRecursive
      - description: Recursively builds inheritance chain; passes filepath.Dir(resolvedPath) as basePath for parent resolution
        file: config.go
        name: buildChainRecursive
      - description: Resolves relative paths against basePath with home directory expansion and directory/file detection
        file: config.go
        name: resolvePath
  cross-references: '[ARCH-CFG_005], [REQ-CFG_005], [REQ-CONFIGURATION], [IMPL-TEST_CFG_005_P1]'
  cross_references:
    - ARCH-CFG_005
    - REQ-CFG_005
    - REQ-CONFIGURATION
  date: '2025-12-11'
  fixed_buildchainrecursive_basepath: Changed line 2289 from `resolvedPath` to `filepath.Dir(resolvedPath)` when passing basePath to recursive calls
  fixed_resolvepath_directory_handling: 'Updated `resolvePath` to:'
  implementation_approach:
    details: []
    summary: '- **Fixed buildChainRecursive basePath**: Changed line 2289 from `resolvedPath` to `filepath.Dir(resolvedPath)` when passing basePath to recursive calls'
  metadata:
    created:
      author: MCP converter
      date: '2026-03-12'
    last_updated:
      author: AI agent
      date: '2026-06-02'
      reason: Track C bulk sidecar migration from inline essence
    last_validated:
      date: '2026-03-12'
      result: pass
      validator: MCP converter
  name: Inheritance Path Resolution Fix
  priority: P1 (Important)
  rationale:
    benefits: []
    problems_solved: []
    why: |-
      Bug fix: `buildChainRecursive` was passing `resolvedPath` (file path) instead of `filepath.Dir(resolvedPath)` (directory) as basePath
      Bug fix: `resolvePath` was not expanding `~` before resolving relative paths
      Bug fix: `resolvePath` was not handling directory paths correctly (was calling `filepath.Dir()` on directories)
      These bugs prevented inheritance chains from resolving parent files correctly, especially with relative paths and home directory expansion
  related_decisions:
    composed_with: []
    depends_on: []
    see_also:
      - ARCH-CFG_005
      - REQ-CFG_005
      - REQ-CONFIGURATION
    supersedes: []
  status: Active
  test_coverage:
    - 'TestConfigInheritance: Now passes with relative path `base.yml` resolving correctly'
    - 'TestHomeDirectoryExpansion_REQ_CFG_005: Tests `~/.bkpdir-base.yml` expansion in inherit paths'
    - 'TestRelativePathInheritance_REQ_CFG_005: Tests `../base/base.yml` and `./sibling.yml` resolution'
  traceability:
    architecture:
      - ARCH-CFG_005
    code_annotations:
      - ARCH-CFG_005
      - IMPL-CFG_INHERITANCE_PATH_RESOLUTION
      - REQ-CFG_005
      - REQ-CONFIGURATION
    requirements:
      - REQ-CFG_005
      - REQ-CONFIGURATION
    tests:
      - TestConfigInheritance
      - TestHomeDirectoryExpansion_REQ_CFG_005
      - TestRelativePathInheritance_REQ_CFG_005
