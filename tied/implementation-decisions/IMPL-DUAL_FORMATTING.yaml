IMPL-DUAL_FORMATTING:
  code_locations:
    files:
      - formatter.go
      - pkg/formatter/collector.go
      - pkg/formatter/formatter.go
      - pkg/formatter/interfaces.go
      - pkg/formatter/patterns.go
      - pkg/formatter/placeholder_replace.go
      - pkg/formatter/template.go
    functions:
      - DefaultOutputFormatter.FormatConfigValue (pkg/formatter/formatter.go)
      - DefaultOutputFormatter.FormatCreatedArchive (pkg/formatter/formatter.go)
      - DefaultOutputFormatter.FormatError (pkg/formatter/formatter.go)
      - DefaultOutputFormatter.FormatIdenticalArchive (pkg/formatter/formatter.go)
      - DefaultOutputFormatter.FormatListArchive (pkg/formatter/formatter.go)
      - DefaultOutputFormatter.PrintCreatedArchive (pkg/formatter/formatter.go)
      - DefaultPatternExtractor.ExtractArchiveFilenameData (pkg/formatter/patterns.go)
      - DefaultPatternExtractor.ExtractPatternData (pkg/formatter/patterns.go)
      - DefaultTemplateFormatter.FormatWithPlaceholders (pkg/formatter/template.go)
      - DefaultTemplateFormatter.FormatWithTemplate (pkg/formatter/template.go)
      - DefaultTemplateFormatter.TemplateCreatedArchive (pkg/formatter/template.go)
      - OutputCollector.AddStdout (pkg/formatter/collector.go)
      - OutputCollector.FlushAll (pkg/formatter/collector.go)
      - OutputFormatter.FormatConfigValue (formatter.go)
      - OutputFormatter.FormatCreatedArchive (formatter.go)
      - OutputFormatter.FormatCreatedBackup (formatter.go)
      - OutputFormatter.FormatDryRunArchive (formatter.go)
      - OutputFormatter.FormatError (formatter.go)
      - OutputFormatter.FormatIdenticalArchive (formatter.go)
      - OutputFormatter.FormatIdenticalBackup (formatter.go)
      - OutputFormatter.FormatListArchive (formatter.go)
      - OutputFormatter.FormatListBackup (formatter.go)
      - OutputFormatter.formatTemplate (formatter.go)
      - OutputFormatter.PrintCreatedArchive (formatter.go)
      - OutputFormatter.PrintError (formatter.go)
      - OutputFormatter.PrintIdenticalArchive (formatter.go)
      - OutputFormatter.PrintListArchive (formatter.go)
      - ReplacePlaceholders (pkg/formatter/placeholder_replace.go)
  code_markers: '`FormatXXX()` and `TemplateXXX()` functions, regex patterns'
  cross_references:
    - ARCH-OUTPUT_FORMATTING
    - REQ-OUTPUT_FORMATTING
  essence_pseudocode: |-
    # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING]
    # Dual-mode output formatting: printf-style for backward-compat, template-style for rich data [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING]

    # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING] — Block implements documented behavior for: procedure format_with_printf(formatType, args...):
    procedure format_with_printf(formatType, args...):
      # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING] Printf formatting path
      formatStr = config.GetFormatString(formatType)
      if formatStr == "":
        formatStr = defaultFormats[formatType]
      return sprintf(formatStr, args...)

    # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING] — Block implements documented behavior for: procedure format_with_template(templateStr, data map):
    procedure format_with_template(templateStr, data map):
      # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING] Template placeholder replacement
      result = templateStr

      # [IMPL-DUAL_FORMATTING] [REQ-CUSTOMIZABLE_FORMAT_STRINGS] Replace #{key} placeholders from data map [ARCH-OUTPUT_FORMATTING]
      for each (key, value) in data:
        placeholder = "#{" + key + "}"
        result = replaceAll(result, placeholder, value)

      # [IMPL-DUAL_FORMATTING] [REQ-CUSTOMIZABLE_FORMAT_STRINGS] Fill known missing placeholders with defaults [ARCH-OUTPUT_FORMATTING]
      knownDefaults = {"#{size_human}": "unknown", "#{size}": "0", "#{mtime}": "unknown"}
      for each (placeholder, default) in knownDefaults:
        if result contains placeholder:
          result = replaceAll(result, placeholder, default)

      # [IMPL-DUAL_FORMATTING] [REQ-CUSTOMIZABLE_FORMAT_STRINGS] Handle mixed printf+template strings [ARCH-OUTPUT_FORMATTING]
      if result contains "%s":
        replace first %s with data["path"] or data["name"]
        replace second %s with data["creation_time"] if present

      # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] Go text/template fallback [REQ-OUTPUT_FORMATTING]
      if result contains "{{" and not "#{":
        parse and execute as Go text/template with data
        on error: return result as-is (graceful fallback)

      return result

    # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING] — Block implements documented behavior for: procedure format_list_archive(path, creationTime):
    procedure format_list_archive(path, creationTime):
      # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING] Dual-mode list formatting
      formatStr = config.GetFormatString("list_archive")
      if formatStr contains "#{":
        # [IMPL-DUAL_FORMATTING] [REQ-CUSTOMIZABLE_FORMAT_STRINGS] Template path with file stats [ARCH-OUTPUT_FORMATTING]
        data = {"path": path, "creation_time": creationTime}
        statInfo = GatherFileStatInfo(path)
        if statInfo != nil:
          merge statInfo fields into data
        else:
          fill data with safe defaults
        return format_with_template(formatStr, data)
      else:
        # [IMPL-DUAL_FORMATTING] [REQ-OUTPUT_FORMATTING] Printf fallback for backward compatibility [ARCH-OUTPUT_FORMATTING]
        return sprintf(formatStr, path, creationTime)

    # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING] — Block implements documented behavior for: procedure print_formatted(formatType, message):
    procedure print_formatted(formatType, message):
      # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [IMPL-DELAYED_OUTPUT] Output routing [REQ-OUTPUT_FORMATTING]
      if collector != nil:
        collector.AddStdout(message, formatType)
      else:
        print(message)

    # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING] — Block implements documented behavior for: procedure extract_pattern_data(pattern, text):
    procedure extract_pattern_data(pattern, text):
      # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] Regex-based data extraction for template processing [REQ-OUTPUT_FORMATTING]
      re = compile(pattern)
      matches = re.FindStringSubmatch(text)
      result = empty map
      for each named group in matches:
        result[name] = match_value
      return result

    # [IMPL-DUAL_FORMATTING] [ARCH-OUTPUT_FORMATTING] [REQ-OUTPUT_FORMATTING] — Block implements documented behavior for: procedure replace_placeholders(formatStr, data):
    procedure replace_placeholders(formatStr, data):
      # [IMPL-DUAL_FORMATTING] [REQ-CUSTOMIZABLE_FORMAT_STRINGS] Simple #{key} replacement [ARCH-OUTPUT_FORMATTING]
      result = formatStr
      for each (key, value) in data:
        result = replaceAll(result, "#{" + key + "}", value)
      return result
  implementation_approach:
    details: []
    summary: |-
      `OutputFormatter`: Printf-style formatting methods
      `TemplateFormatter`: Advanced template processing
      Regex extraction for rich data formatting
      Support for both Go text/template and placeholder syntax
  metadata:
    created:
      author: MCP converter
      date: "2026-03-12"
    last_updated:
      author: AI agent
      date: "2026-03-12"
      reason: Enriched pseudo-code, populated code_locations and traceability.tests per PROC-IMPL_CODE_TEST_SYNC
    last_validated:
      date: "2026-03-12"
      result: pass
      validator: MCP converter
  name: Dual Printf/Template Formatting
  rationale:
    benefits: []
    problems_solved: []
    why: |-
      Printf for simple, backward-compatible formatting
      Templates for rich data extraction and advanced formatting
      Graceful fallback from template to printf
      Supports ANSI colors and structured output
  related_decisions:
    composed_with: []
    depends_on: []
    see_also:
      - ARCH-OUTPUT_FORMATTING
      - IMPL-CUSTOMIZABLE_FORMAT_STRINGS
      - IMPL-DELAYED_OUTPUT
      - IMPL-LIST_FORMAT_SAFETY
      - REQ-OUTPUT_FORMATTING
    supersedes: []
  status: Active
  traceability:
    architecture:
      - ARCH-OUTPUT_FORMATTING
    code_annotations:
      - ARCH-OUTPUT_FORMATTING
      - IMPL-DUAL_FORMATTING
      - REQ-OUTPUT_FORMATTING
    requirements:
      - REQ-OUTPUT_FORMATTING
    tests:
      - formatter_test.go::TestTemplateFormatter
      - pkg/formatter/formatter_test.go::TestDefaultOutputFormatter
      - pkg/formatter/formatter_test.go::TestDelayedOutputMode
      - pkg/formatter/formatter_test.go::TestErrorTemplateFormatting
      - pkg/formatter/formatter_test.go::TestInterfaceCompliance
      - pkg/formatter/formatter_test.go::TestOutputCollector
      - pkg/formatter/formatter_test.go::TestPatternExtractionEdgeCases
      - pkg/formatter/formatter_test.go::TestPatternExtractor
      - pkg/formatter/formatter_test.go::TestTemplateDelegation
      - pkg/formatter/formatter_test.go::TestTemplateFormatter
      - pkg/formatter/placeholder_replace_test.go::TestReplacePlaceholders_AllKnown
      - pkg/formatter/placeholder_replace_test.go::TestReplacePlaceholders_EmptyData
      - pkg/formatter/placeholder_replace_test.go::TestReplacePlaceholders_ExactBugScenario
      - pkg/formatter/placeholder_replace_test.go::TestReplacePlaceholders_Multiple
      - pkg/formatter/placeholder_replace_test.go::TestReplacePlaceholders_MultipleOccurrences
      - pkg/formatter/placeholder_replace_test.go::TestReplacePlaceholders_NoPlaceholders
      - pkg/formatter/placeholder_replace_test.go::TestReplacePlaceholders_Simple
      - pkg/formatter/placeholder_replace_test.go::TestReplacePlaceholders_SpecialChars
      - pkg/formatter/placeholder_replace_test.go::TestReplacePlaceholders_UnknownPlaceholder
